package Inheritance.HybridInheritance.RestaurantManagementSystem;
public interface Worker {

    /*
     This interface represents a worker role in the restaurant.
     It defines a contract that requires implementing classes
     to specify how their duties are performed.
    */

    // Performs role-specific duties
    void performDuties();
}
